Act as a senior AWS Solutions Architect and enterprise architecture-diagram designer.

Analyze the complete project at C:\Users\hardi\self-healing-ec2-fleet. Read README.md, PLAN.md, ARCHITECTURE.md, and all infrastructure source files before drawing. Classify every resource as IMPLEMENTED, PLANNED, or EXTERNAL. Mark planned resources with dashed borders and a visible PLANNED label; never present a planned resource as deployed.

Use Miro MCP diagram_create to add the completed diagram to:
https://miro.com/app/board/REPLACE_WITH_BOARD_ID/

Create an editable AWS deployment and infrastructure architecture diagram titled "Self-Healing EC2 Fleet - AWS Technical Architecture" with the subtitle "Multi-AZ compute resilience using ALB health checks and Auto Scaling lifecycle automation."

This must be an infrastructure architecture diagram, not a flowchart, workflow, mind map, UML activity diagram, or business-process diagram. Do not use Start, End, Process, Input/Output, decision-diamond, or generic workflow symbols. Do not arrange AWS services as a sequential process, number services as workflow steps, use swimlanes for AWS infrastructure, duplicate services for lifecycle stages, or represent Regions, VPCs, Availability Zones, and subnets as process boxes. Use containment to show deployment and connectors only to show communication. If behavioral detail is necessary, create a separate sequence diagram beside the architecture diagram.

Use a professional 16:9 landscape canvas with a white background. Arrange users and infrastructure delivery on the left, the AWS workload topology in the center, the self-healing and observability plane below the workload, operations recipients on the right, and a compact legend at the bottom.

Build this nested infrastructure hierarchy:

AWS Cloud
  AWS Region: us-east-1
    VPC: 10.0.0.0/16
      Edge and ingress
      Availability Zone A
        Public subnet: 10.0.0.0/24
        Private subnet: 10.0.10.0/24
      Availability Zone B
        Public subnet: 10.0.1.0/24
        Private subnet: 10.0.11.0/24
      Availability Zone C
        Public subnet: 10.0.2.0/24
        Private subnet: 10.0.12.0/24
      Regional self-healing and operations services

Outside AWS on the left, place Application Users, Platform/SRE Team, and a Terraform or CI/CD workstation only when verified.

Inside the AWS Region and VPC, place Amazon Route 53, Internet Gateway, Application Load Balancer, ALB listener, Target Group, three Availability Zones, the public/private subnet pairs, NAT Gateway in each public subnet only when verified, one Auto Scaling Group spanning all three private subnets, EC2 instances distributed across the private subnets, a Launch Template attached to the Auto Scaling Group, and compact ALB and EC2 security-group annotations.

EC2 instances must remain inside private subnets, must not have public IP addresses, and must accept application traffic only from the ALB security group. The Auto Scaling Group must be one logical boundary spanning the private subnets in all three Availability Zones.

Below the workload, inside the Region but outside all subnets, place Amazon EventBridge, Terminate Handler Lambda, Launch Handler Lambda, AWS Systems Manager Automation, Amazon S3 archive, Amazon CloudWatch, AWS CloudTrail, Amazon SNS, and least-privilege IAM roles as attached security annotations. Never place Lambda, EventBridge, Systems Manager, S3, CloudWatch, CloudTrail, SNS, or IAM inside the Auto Scaling Group.

Outside AWS on the right, place the On-call Engineer/Operations Team and a recovery outcome annotation.

Draw direct, orthogonal, labeled relationships with exactly one source and one target.

Normal request path:
- Application Users -> Route 53: DNS lookup
- Application Users/Internet -> Application Load Balancer: HTTPS 443
- Application Load Balancer -> Target Group: listener forwards request
- Target Group -> EC2 fleet: application traffic
- Application Load Balancer -> EC2 fleet: target health checks

Fleet configuration:
- Launch Template -> Auto Scaling Group: AMI, instance type, IAM, user data
- Auto Scaling Group -> EC2 instances: maintains desired capacity
- Auto Scaling Group -> Target Group: registers and deregisters targets

Termination lifecycle:
- Auto Scaling Group -> EventBridge: EC2_INSTANCE_TERMINATING / Terminating:Wait
- EventBridge -> Terminate Handler Lambda: matched lifecycle event
- Terminate Handler Lambda -> Target Group: deregister target and drain
- Terminate Handler Lambda -> Amazon S3: archive logs/session state
- Terminate Handler Lambda -> Auto Scaling Group: CompleteLifecycleAction(CONTINUE)

Replacement lifecycle:
- Auto Scaling Group -> EC2 fleet: launch replacement
- Auto Scaling Group -> EventBridge: EC2_INSTANCE_LAUNCHING / Pending:Wait
- EventBridge -> Launch Handler Lambda: matched lifecycle event
- Launch Handler Lambda -> Systems Manager Automation: start automation runbook
- Systems Manager Automation -> replacement EC2 instance: bootstrap, configure, verify readiness
- Launch Handler Lambda -> Auto Scaling Group: CompleteLifecycleAction(CONTINUE)
- Auto Scaling Group -> Target Group: register healthy replacement

Observability and notification:
- ALB, Auto Scaling Group, EC2, Lambda, and Systems Manager -> CloudWatch: metrics, logs, alarms
- AWS management API activity -> CloudTrail: audit events
- Lambda handlers -> SNS: recovery notification
- SNS -> On-call Engineer: email/ChatOps alert
- Terraform or CI/CD -> AWS resources: deployment and configuration

Connector styles:
- Solid dark line: request and data-plane traffic
- Dashed purple line: lifecycle events and remediation
- Dotted gray line: metrics, logs, telemetry, and audit
- Dashed red line: alarms and operational notifications
- Dashed blue line: deployment and management

Use arrowheads and orthogonal routing. Avoid diagonal lines, crossings, and connectors passing through cards, labels, icons, or boundary headings. Route telemetry behind primary traffic.

Use official AWS Architecture Icons when available and use one consistent icon generation. Do not mix AWS4, AWS 3D, legacy, and generic icon styles. Use AWS orange for compute and lifecycle emphasis, green outlines for VPC and subnet boundaries, blue for ingress and management, purple for automation, and red only for alerts and security restrictions. Use white cards with subtle borders, short labels, consistent dimensions, padding, typography, whitespace, and grid alignment. Put CIDRs, ports, protocols, and lifecycle states in secondary labels. Make the request path visually dominant and keep lifecycle automation below it.

Add this recovery outcome annotation:
"Unhealthy target isolated -> connections drained -> replacement launched -> configuration applied -> readiness verified -> healthy target registered"

Generation procedure:
1. Create the nested Cloud, Region, VPC, Availability Zone, subnet, and Auto Scaling Group boundaries.
2. Place each service exactly once in its correct deployment boundary.
3. Add the normal request path.
4. Add fleet configuration relationships.
5. Add lifecycle remediation below the workload.
6. Add telemetry, audit, notification, and deployment relationships.
7. Add security annotations and the legend.
8. Read the diagram back and correct containment errors, overlaps, crossed connectors, inconsistent sizing, and unreadable labels.

If diagram_create cannot express the nested architecture cleanly, use layout_create with frames, shapes, official AWS icons or images, text, and connectors. Do not fall back to a flowchart.

Before finishing, verify that the result is immediately recognizable as an AWS infrastructure architecture; all infrastructure boundaries are nested correctly; no flowchart symbols exist; every service appears only once; EC2 instances are inside private subnets and the Auto Scaling Group; regional managed services remain outside subnets; every connector has a source, target, label, and correct style; the request path reads left to right; planned resources are clearly distinguished; no line crosses a component label or boundary heading; and every object remains editable.
